AWSTemplateFormatVersion: "2010-09-09"
Description: >
  ReRhythm - Storage Infrastructure
  S3 + DynamoDB + VPC

Parameters:
  EnvironmentName:
    Type: String
    Default: rerythm-prod

Resources:

  # ─────────────────────────────────────────
  # NETWORKING
  # ─────────────────────────────────────────
  ReRhythmVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.0.0.0/16
      EnableDnsSupport: true
      EnableDnsHostnames: true
      Tags:
        - Key: Name
          Value: !Sub "${EnvironmentName}-vpc"

  PublicSubnet1:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ReRhythmVPC
      CidrBlock: 10.0.1.0/24
      AvailabilityZone: !Select [0, !GetAZs ""]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub "${EnvironmentName}-public-subnet-1"

  InternetGateway:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
        - Key: Name
          Value: !Sub "${EnvironmentName}-igw"

  VPCGatewayAttachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      VpcId: !Ref ReRhythmVPC
      InternetGatewayId: !Ref InternetGateway

  PublicRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref ReRhythmVPC
      Tags:
        - Key: Name
          Value: !Sub "${EnvironmentName}-public-rt"

  PublicRoute:
    Type: AWS::EC2::Route
    DependsOn: VPCGatewayAttachment
    Properties:
      RouteTableId: !Ref PublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref InternetGateway

  PublicSubnet1RouteTableAssoc:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PublicSubnet1
      RouteTableId: !Ref PublicRouteTable

  # ─────────────────────────────────────────
  # S3
  # ─────────────────────────────────────────
  ResumeUploadBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "${EnvironmentName}-resume-uploads-${AWS::AccountId}"
      VersioningConfiguration:
        Status: Enabled
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      LifecycleConfiguration:
        Rules:
          - Id: ExpireOldResumes
            Status: Enabled
            ExpirationInDays: 30
      CorsConfiguration:
        CorsRules:
          - AllowedHeaders: ["*"]
            AllowedMethods: [GET, PUT, POST]
            AllowedOrigins: ["*"]
            MaxAge: 3000

  # ─────────────────────────────────────────
  # DYNAMODB
  # ─────────────────────────────────────────
  RoadmapResponseTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${EnvironmentName}-roadmap-responses"
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: createdAt
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: createdAt
          KeyType: RANGE
      SSESpecification:
        SSEEnabled: true
      TimeToLiveSpecification:
        AttributeName: ttl
        Enabled: true

  LessonPlanTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${EnvironmentName}-lesson-plans"
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: moduleId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: moduleId
          KeyType: RANGE
      SSESpecification:
        SSEEnabled: true

  BadgeAchievementTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub "${EnvironmentName}-badge-achievements"
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
        - AttributeName: badgeId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
        - AttributeName: badgeId
          KeyType: RANGE
      SSESpecification:
        SSEEnabled: true



# ─────────────────────────────────────────
# OUTPUTS
# ─────────────────────────────────────────
Outputs:
  ResumeUploadBucketName:
    Description: S3 Resume Bucket
    Value: !Ref ResumeUploadBucket
    Export:
      Name: !Sub "${EnvironmentName}-resume-bucket"

  DynamoDBRoadmapTable:
    Description: DynamoDB Roadmap Table
    Value: !Ref RoadmapResponseTable
    Export:
      Name: !Sub "${EnvironmentName}-roadmap-table"

  DynamoDBLessonTable:
    Description: DynamoDB Lesson Table
    Value: !Ref LessonPlanTable
    Export:
      Name: !Sub "${EnvironmentName}-lesson-table"

  DynamoDBBadgeTable:
    Description: DynamoDB Badge Achievement Table
    Value: !Ref BadgeAchievementTable
    Export:
      Name: !Sub "${EnvironmentName}-badge-table"
